/*
====================================================

416. Partition Equal Subset Sum

Given an integer array nums, return true if you can partition the array into two subsets such that the sum of the elements in both subsets is equal or false otherwise.

Example 1:

Input: nums = [1,5,11,5]
Output: true

Explanation:
The array can be partitioned as [1,5,5] and [11].

Example 2:

Input: nums = [1,2,3,5]
Output: false

Explanation:
The array cannot be partitioned into equal sum subsets.

Constraints:

1 <= nums.length <= 200
1 <= nums[i] <= 100

====================================================

Brute Force : give TLE

class Solution {
public:
    bool solve(int i, vector<int>& nums, int x)
    {
        if(x==0)
            return true;

        if(i>=nums.size())
            return false;

        bool nottake=solve(i+1, nums, x);

        bool take;
        if(x>=nums[i])
            take=solve(i+1, nums, x-nums[i]);

        return nottake || take;
    }

    bool canPartition(vector<int>& nums) {
        int n=nums.size();

        int sum=accumulate(begin(nums), end(nums), 0);

        if(sum%2!=0)
            return false;

        int x=sum/2;

        return solve(0, nums, x);
    }
};

====================================================

Memozation:

class Solution {
public:
    int dp[201][10001];

    bool solve(int i, vector<int>& nums, int x)
    {
        if(dp[i][x]!=-1)
            return dp[i][x];

        if(x==0)
            return true;

        if(i>=nums.size())
            return false;

        bool nottake=solve(i+1, nums, x);

        bool take;
        if(x>=nums[i])
            take=solve(i+1, nums, x-nums[i]);

        return dp[i][x]=nottake || take;
    }

    bool canPartition(vector<int>& nums) {
        int n=nums.size();

        memset(dp,-1,sizeof(dp));

        int sum=accumulate(begin(nums), end(nums), 0);

        if(sum%2!=0)
            return false;

        int x=sum/2;

        return solve(0, nums, x);
    }
};

====================================================
*/
