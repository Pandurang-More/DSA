class Solution {
  public:
    // Function to remove duplicates from the given array.
    vector<int> removeDuplicates(vector<int> &arr) {
        
        int i=0;
        int n = arr.size();
        for(int j=1;j<n;j++){
            if(arr[j]!=arr[i]){
                arr[i+1]=arr[j];
                i++;
            }
           
        }
         arr.resize( i+1);
         return arr;
    }
};

we can use set function but time complexity will be O(nlogn) as one operation in set takes logn time.
