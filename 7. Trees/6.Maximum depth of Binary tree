class Solution {
public:
    int maxDepth(TreeNode* root) {
        if(root==NULL)
        return 0;
        int maxi=1+ max(maxDepth(root->left),maxDepth(root->right));
        return maxi;
        
    }
};


Input: root = [3,9,20,null,null,15,7]
Output: 3
Example 2:

Input: root = [1,null,2]
Output: 2

depth of one node is 1.
